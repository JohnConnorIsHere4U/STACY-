contract ExampleVault:

    function deposit(amount):
        REQUIRE: amount > 0
        balance += amount

    function withdraw(amount):
        REQUIRE: balance >= amount
        balance -= amount